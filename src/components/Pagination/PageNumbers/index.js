import React from "react";
import { NumberButton } from "../styles";
import { v4 as uuid } from "uuid";

const PageNumbers = ({ paging, setPageNumber }) => {
  const { pageNumber, totalPages, recordsPerPage } = paging;
};

export default PageNumbers;
